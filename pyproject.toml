[project]
name = "netgame"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Veronica Li Aguirre", email = "veronicali@princeton.edu" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
